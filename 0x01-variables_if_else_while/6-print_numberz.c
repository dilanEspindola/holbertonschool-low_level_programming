#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - numbers 0 to 9
 *
 * Return: (0)
 */
int main(void)
{
	int number = 48;

	while (number <= 57)
	{
		putchar(number);
		number++;
	}
	putchar('\n');
	return (0);
}
