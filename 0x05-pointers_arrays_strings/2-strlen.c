#include <stdio.h>
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: is a pointer
 * Return: length of a string
*/

int _strlen(char *s)
{
	int lengthString;

	while (*(s + lengthString))
		lengthString++;

	return (i);
}
